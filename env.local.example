# 🍴 The Chef's Numbers - Lokale PostgreSQL + PostgREST Konfiguration
# Kopieren Sie diese Datei zu .env.local und passen Sie die Werte an

# ====================================
# LOKALE POSTGRESQL + POSTGREST KONFIGURATION
# ====================================

# PostgREST API URL (Port 3000 aus docker-compose-supabase.yml)
REACT_APP_SUPABASE_URL=http://localhost:3000

# Anon Key (kann beliebig sein für lokale Installation)
REACT_APP_SUPABASE_ANON_KEY=local-anon-key-for-postgrest

# Service Role Key (kann beliebig sein für lokale Installation)
REACT_APP_SUPABASE_SERVICE_ROLE_KEY=local-service-role-key-for-postgrest

# ====================================
# ALTERNATIVE: SUPABASE CLOUD KONFIGURATION
# ====================================
# Falls Sie Supabase Cloud verwenden möchten, kommentieren Sie die obigen Zeilen aus
# und verwenden Sie diese Konfiguration:

# REACT_APP_SUPABASE_URL=https://ihr-projekt.supabase.co
# REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# REACT_APP_SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ====================================
# HINWEISE
# ====================================

# 1. Für lokale Installation:
#    - URL: http://localhost:3000 (PostgREST Port)
#    - Keys: Können beliebig sein, werden nur für Authentifizierung verwendet
#    - Init-Scripts werden automatisch geladen aus ./init-scripts/

# 2. Für Supabase Cloud:
#    - URL: https://ihr-projekt.supabase.co
#    - Keys: Echte JWT-Tokens aus dem Supabase Dashboard

# 3. Docker-Container starten:
#    docker-compose -f docker-compose-supabase.yml up -d

# 4. Dashboard verfügbar unter:
#    http://localhost:8000 (Supabase Studio)
