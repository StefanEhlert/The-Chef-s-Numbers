# Supabase Self-Hosting Umgebungsvariablen für The Chef's Numbers
# Diese Datei kann direkt in Portainer.io hochgeladen werden

# PostgreSQL Datenbank-Konfiguration
POSTGRES_PASSWORD=chef_password_123
POSTGRES_USER=chef_user
POSTGRES_DB=chef_numbers

# JWT Secret (WICHTIG: Ändern Sie diesen Wert für Produktion!)
# Generieren Sie einen neuen mit: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long-change-this

# Supabase URLs (ändern Sie localhost zu Ihrer Server-IP)
SUPABASE_URL=http://localhost:8000
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# Hinweis: Die Supabase-Schlüssel werden automatisch generiert
# und können nach dem ersten Start aus dem Dashboard abgerufen werden
# Dashboard: http://localhost:8000 -> Settings -> API
